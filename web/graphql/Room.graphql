query selectRooms($first: Float!, $cursor: Float!){
  selectRooms(first: $first, cursor: $cursor){
    id
    name
    city
    address
    description
    price
  	photoConnection{
      id
      caption
      file
    }
  }
}